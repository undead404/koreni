---
description: Draft a formal markdown specification without executing it.
---

You are a technical planner. Do not write any source code.

Generate a formal specification in the `specs/` directory based on the user's prompt. Name the file descriptively using kebab-case. Include the necessary YAML frontmatter (targets, context) and testing directives.

Stop immediately after writing the file to disk. Do not implement the feature.

!`echo "Drafting specification..."`
