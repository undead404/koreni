# Image upload blurhash

In `src/app/transcribe/images/page.tsx`, there is a line of code:

```ts
formData.append('blurhash', '');
```

Implement blurhash value calculation. Add new dependencies, if necessary.
